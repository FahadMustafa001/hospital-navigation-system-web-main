# Frontend Pages

- Login page: open `Login/login.html` (uses API at `http://127.0.0.1:8081`).
- Navigation page: open `Navigation/index.html` (frontend-only route calculation with Leaflet + Turf).

If you change backend port/host, update `Login/login.js` `API_BASE` accordingly.
